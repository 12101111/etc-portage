diff --git a/src/util/sockopt.c b/src/util/sockopt.c
index 81e19dd..639c59e 100644
--- a/src/util/sockopt.c
+++ b/src/util/sockopt.c
@@ -136,6 +136,10 @@ int sockopt_get_peergroups(int fd, Log *log, uid_t uid, gid_t primary_gid, gid_t
  *         support the option, SOCKOPT_E_REAPED if the target process was
  *         already reaped, negative error code on failure.
  */
+#ifndef SO_PEERPIDFD
+#define SO_PEERPIDFD 77
+#endif
+
 int sockopt_get_peerpidfd(int fd, int *pidfdp) {
         socklen_t socklen = sizeof(int);
         int r, pidfd;
